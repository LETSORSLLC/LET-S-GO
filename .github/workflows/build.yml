1. **Update package.json:**
   Ensure that your package.json file includes the test script:
   json
   {
     "scripts": {
       "test": "jest"
     }
   }
   

2. *Commit and Push Changes:*
   sh
   git add package.json
   git commit -m "Install jest and update test script"
   git push origin main
   

3. *Re-run the Workflow:*
